<!DOCTYPE html>
<html lang="en" class="bg-gray-200">

<head>
    <meta charset="UTF-8">
    <title>
        <%= locals.title || "ProjMate" %>
    </title>
    <link rel="stylesheet" href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css">
    <link rel="shortcut icon" href="//cdn2.auth0.com/styleguide/latest/lib/logos/img/favicon.png">
</head>

<body>
    <nav class="flex items-center justify-between flex-wrap bg-gray-800 p-6">

        <div class="flex items-center flex-shrink-0 text-white mr-6">
            <img src="/Logos/ProjMate.png" alt="ProjMate Icon" class="fill-current h-14 w-8"></img></div>
            <span class="font-semibold text-xl tracking-tight" style="color: white; padding-right: 1cm;">ProjMate</span>
        </div>

        <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
            <div class="text-sm lg:flex-grow">
                <a href="/" class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Home</a>
                <% if (locals.user) { %>
                    <a href="/projects"
                        class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">My Projects</a>
                    <a href="/logout"
                        class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Logout</a>
                    <% } else { %>
                        <a href="/login"
                            class="block mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Login</a>
                        <% } %>
            </div>

            <div>
                <a href="https://github.com/auth0/express-openid-connect" target="blank"
                    class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-gray-800 hover:bg-white mt-4 lg:mt-0">
                    <span class="font-semibold text-xl tracking-tight">GitHub</span>

                </a>
            </div>
        </div>
    </nav>
    <div class="flex justify-center py-4">
        <div class="w-5/6 lg:w-3/4 bg-white p-4 rounded overflow-hidden shadow-lg">